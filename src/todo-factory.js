function createToDo(title, description, dueDate, priority, complete){
    return {
        title,
        description,
        dueDate,
        priority,
        complete
    };
};




export {createToDo};